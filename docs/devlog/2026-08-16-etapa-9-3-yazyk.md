# 2026-08-16 — Промежуточный 9.3: язык

## Сделано

- i18next + react-i18next: словари `ru` / `en`.
- Настройки: переключатель языка (Русский / English); значение в AsyncStorage (`gonext.language`).
- Сохранённый язык загружается до первого кадра UI (вместе с темой).
- Подписи экранов, чипы, алерты, формы, даты и склонения идут через `t()`.

## Проверка

- `npx tsc --noEmit` — без ошибок.
- Смена языка сразу обновляет интерфейс; переживает перезапуск; тема и цвет не сбрасываются.

## Время

- План: не выставлялся
- Факт: ~1 ч 10 настенных
