# 2026-08-16 — Промежуточный 9.2: цвет темы

## Сделано

- Настройки: десять кружков основного цвета (лес / бирюза / океан / индиго / фиолет / ягода / терракота / янтарь / олива / сланец).
- Значение в AsyncStorage (`gonext.themeAccent`); по умолчанию лес `#3f5c46`.
- Цвет идёт в Paper `colors.primary` и в семейство акцента UI (кнопки, чипы, FAB).
- Версия `__version__` → `0.10.2`.

## Проверка

- `npx tsc --noEmit` — без ошибок.
- Переключение кружка сразу меняет акцентные кнопки; переживает перезапуск; светлая/тёмная тема не сбрасывает цвет.

## Время

- План: не выставлялся
- Факт: ~0 ч 20 настенных
