# 2026-08-16 — Этап 7: Следующее место

## Сделано

- Очередь: первое `pending` по `sortOrder`; `visited` и `skipped` пропускаются.
- Экран `/next`: карточка следующего места или «Все места пройдены».
- Карта / навигатор (`geo:` и Apple Maps) и копирование координат с toast.
- «Посещено» → мини-форма (заметка, «понравилось» + sync в Place, заготовка фото к этапу 8).
- «Пропустить» сразу считает следующее; skip не ломает очередь.
- Главная: кнопка «Навигатор» открывает следующее pending.
- Версия `__version__` → `0.8.0`.

## Проверка

- `npx tsc --noEmit` — без ошибок.
- Критерий: демо «Карелия» — следующее → посещено → следующее; skip Рускеалы ведёт на Кижи.

## Время

- План: 4 ч
- Факт: ~0 ч 45 настенных
