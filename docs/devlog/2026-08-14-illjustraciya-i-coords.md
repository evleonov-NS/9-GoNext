# 2026-08-14 — Иллюстрация hero + вставка координат

## Сделано

- Главная: hero-иллюстрация (`HeroBackdrop` + `FadeToBackground`), стеклянная шапка, карточка поездки на границе растворения (измерение `onLayout`, без полосы стыка).
- Ассеты: cover/card/bg; палитра из `assets/INTEGRATION_PROMPT.md` в `constants/theme.ts`.
- Обложки поездки/идеи/дневника и compact-карточек идей; illustrated empty states.
- Сплэш / boot DB: полноэкранный `gonext-bg-*`, фон `#faf4e9`.
- Форма места: «Вставить координаты из буфера» + `parseCoordsPair` (DD); на web — запасное поле Ctrl+V.
- Версия `__version__` → `0.6.1`.

## Проверка

- `npx tsc --noEmit` — без ошибок.
- Приёмка hero: нет видимой границы картинка/фон; карточка без пустого зазора; на телефоне — ок.

## Время

- План: не выставлялся
- Факт: ~1 ч 00 настенных (оценка)
