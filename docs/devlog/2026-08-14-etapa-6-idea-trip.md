# 2026-08-14 — Этап 6: TripIdea → Trip

## Сделано

- Карточка идеи: «Создать поездку» открывает форму дат (`/form/trip?ideaId=`).
- Форма: название и описание из идеи, даты необязательны; после подтверждения — Trip + TripPlace.
- `services/convertIdeaToTrip`: транзакция; Place не копируется (только `placeId`); `priority` и `notes` переносятся; идея → `converted`.
- Политика повтора: кнопка «Создать ещё одну поездку»; каждый раз новый независимый Trip.
- Версия `__version__` → `0.7.0`.

## Проверка

- `npx tsc --noEmit` — без ошибок.
- Критерий: демо «Стамбул» → поездка с местами и приоритетами; идея converted; повтор создаёт вторую поездку.

## Время

- План: 2 ч
- Факт: ~0 ч 15 настенных
