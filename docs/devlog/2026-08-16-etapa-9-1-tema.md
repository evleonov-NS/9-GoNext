# 2026-08-16 — Промежуточный 9.1: светлая / тёмная тема

## Сделано

- Настройки: выбор «Светлая» / «Тёмная»; значение в AsyncStorage (`gonext.themeScheme`).
- `ThemeProvider` + палитры `lightColors` / `darkColors`; Paper и StatusBar следуют схеме.
- Тёмная тема: скрыты hero, сплэш БД, illustrated empty и обложки-пейзаж (`CoverImage`).
- Версия `__version__` → `0.10.1`.

## Проверка

- `npx tsc --noEmit` — без ошибок.
- Переключение в Настройках, переживание перезапуска, возврат светлой с hero.

## Время

- План: не выставлялся
- Факт: ~0 ч 10 настенных
