# 2026-08-16 — Этап 8: Фотографии и заметки

## Сделано

- Выбор из галереи (несколько) и камеры; файл копируется в `documentDirectory/photos`, в SQLite пишется URI.
- Привязка: посещение — `tripPlaceId` + `placeId`; карточка места — `placeId`; sheet маршрута — оба.
- Просмотр (тап) и удаление с подтверждением: файл в FS + запись в `photos`.
- Заметки посещения (этап 7) остаются; фото рядом на мини-форме, в карточке места и в sheet поездки.
- `expo-image-picker`, `expo-file-system` (SDK 54). Версия `__version__` → `0.9.0`.

## Проверка

- `npx tsc --noEmit` — без ошибок.
- Критерий: добавить фото к посещению → убить приложение → фото на месте; удаление чистит файл.

## Время

- План: 3 ч
- Факт: ~0 ч 20 настенных
