# 2026-08-12 — Этап 1: каркас UI «как прототип»

## Сделано

- Тема Paper + токены палитры (`constants/theme.ts`): акцент `#2f6d58`, фон `#f5f5f2`, карточки/бордеры как в `docs/reference/gonext_prototype.html`.
- 8 категорий мест с цветами и SVG-path иконок (`constants/categories.ts` + `CategoryIcon`).
- Tabs: Главная · Хочу · Поездки · Места — плавающая нижняя панель и иконки из прототипа.
- Статичные экраны по структуре прототипа + заготовки: settings, next, diary, picker, place/idea/trip cards, form/*.
- FAB (+) и bottom sheet «Что добавить?» → формы идеи / места / поездки (без БД).
- Добавлен `react-native-svg` (Expo 54). Версия `__version__` → `0.2.0`.

## Проверка

- `npx tsc --noEmit` — без ошибок.
- Metro уже на `exp://…:8081` (перезагрузка Expo Go / web).

## Время

- План: 6 ч
- Факт: ~1 ч 20 настенных (оценка по сессии; метки начала не было — ориентир, не секундомер)
