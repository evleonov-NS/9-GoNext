# 2026-08-12 — Этап 4: «Хочу» (TripIdea)

## Сделано

- Вкладка «Хочу»: направления (идеи) + «Что посетить» (`visitLater`); поиск с раскладкой ЙЦУКЕН↔QWERTY; фильтры Активные / Все / Архив.
- Карточка идеи: список мест с priority, bottom sheet (priority / notes / ↑↓ / убрать), «+ Добавить место».
- Picker: мультивыбор из Place, уже добавленные скрыты, дубли не создаются (`UNIQUE` + skip в bulk).
- Форма create/edit идеи: title, description, status active/converted/archived; удаление идеи.
- «Создать поездку» — заглушка до этапа 6.
- Версия `__version__` → `0.5.0`.

## Проверка

- `npx tsc --noEmit` — без ошибок.
- Критерий: идея ссылается на Place через placeId; повторное добавление того же места в идею невозможно.

С **этапа 5** ритуал: чек-лист владельцу → «кип» → коммит/пуш → следующий этап в **новом чате** (`PLAN.md` / `README.md`).

## Время

- План: 5 ч
- Факт: ~1 ч 15 настенных (метка начала не ставилась)
